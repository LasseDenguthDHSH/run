package src.platform;

import src.level.Level;

public class EntityPlatform extends Platform {
    public EntityPlatform(int x, int y, int width, int height, Level level) {
        super(x, y, width, height, level);
    }
}
